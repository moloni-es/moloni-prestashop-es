{% extends '@PrestaShop/Admin/layout.html.twig' %}

{% block content %}
    <div class="card">
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item {{ tabActive == 'index' ? 'active' : '' }}">
                        <a class="nav-link" href="{{ path('moloni_es_settings_index') }}">{{ 'Documents'|trans({}, 'Modules.Moloniprestashopes.Settings') }}<span class="sr-only">(current)</span></a>
                    </li>
                    <li class="nav-item {{ tabActive == 'products' ? 'active' : '' }}">
                        <a class="nav-link" href="{{ path('moloni_es_settings_products') }}">{{ 'Products & Customers'|trans({}, 'Modules.Moloniprestashopes.Settings') }}</a>
                    </li>
                    <li class="nav-item {{ tabActive == 'auto' ? 'active' : '' }}">
                        <a class="nav-link" href="{{ path('moloni_es_settings_automation') }}">{{ 'Automation'|trans({}, 'Modules.Moloniprestashopes.Settings') }}</a>
                    </li>
                    <li class="nav-item {{ tabActive == 'tools' ? 'active' : '' }}">
                        <a class="nav-link" href="{{ path('moloni_es_settings_tools') }}">{{ 'Tools'|trans({}, 'Modules.Moloniprestashopes.Settings') }}</a>
                    </li>
                </ul>
            </div>
        </nav>
    </div>
{% block settings %}

{% endblock %}
{% endblock %}

{% block javascripts %}
    {{ parent() }}

    <script src="{{ asset('themes/new-theme/public/cms_page_form.bundle.js') }}"></script>
{% endblock %}