query priceClasses($companyId: Int!,$options: PriceClassOptions) {
  priceClasses(companyId: $companyId,options: $options) {
    data {
      priceClassId
      visible
      name
    }
    errors {
      field
      msg
    }
  }
}

